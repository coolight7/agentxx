JSON 重构任务继续执行总结（可直接交接下会话）

基线   9ec9e95 ，HEAD   f03d1885  +  工作区约   194 files changed, +5543/-3096  未提交。   其中已暂存约190文件（历史重构），本会话新增未暂存8文件：   AGENTS.md / TODOS.md /
wire_protocol.h / anthropic_provider.h / wire_protocol.cpp / openai_provider.cpp / docs/zh-cn/design.md/plugins.md / docs/en/design.md/plugins.md 。   lib shared/static
 通过， client  通过， test_repo  零error，二进制已同步到   agent/build/linux-debug/exec/ ，exec全量   18039 passed / 0 failed 。

1. 本会话已完成

P1：SSE/高频  JsonView（ json.md §4.3 ）✅

核心原则： 先   JsonView::parse  零拷贝路由，只读命中；命中后按需物化   to_json() ，不改变原容错语义 。

  •  agent/lib/src/protocol/openai_provider.cpp ：
    •  加   #include "agentxx/util/json_view.h" 。
    •  新增  View  辅助： viewToString/viewStrField/viewOutputIndex/viewIntField/parseUsageView ，与   jsonStrField/safeOutputIndex/jsonIntField/parseUsage  同语义。
      •  viewToString ：string原样，数字经   to_json().dump() ，bool转 true/false ，null/缺失为空，与原   dump 归一化一致。
      •  viewIntField ：兼容数字+字符串数字， from_chars 解析。
    •  processSseLine （Chat  Completions）重写为  View  路径：
      •  usage/choices[0]/delta/tool_calls/index/id/function.name/arguments  全走  View。
      •  finish_reason  仅字符串生效， content/reasoning_content/thinking/reasoning  仅字符串生效， tool_calls.id  数字经   viewToString 归一化。
    •  processResponsesSseLine （Responses）重写为  View  路径：
      •  usage 顶层/ response.usage 双兼容经   parseUsageView 。
      •  type 路由， response.output_text.delta/reasoning_text.delta/function_call_arguments.*  仅字符串生效。
      •  function_call 经   viewOutputIndex ， reasoning encrypted_content 命中后才组   util::Json 经  bridge  入   extra ， done 载体信号语义与原一致。
    •  safeOutputIndex 加   [[maybe_unused]] （SSE不再用，保留供非流式路径）。
    •  非流式   completeAsync/completeAsyncResponses/extractApiError/extractToolCalls  仍   Json::parse ，属低频正确路径，不改。
  •  agent/lib/include/agentxx/protocol/anthropic_provider.h ：
    •  加   json_view.h ， processSseBlock  重写为  View  路径。
    •  message_start/content_block_start/content_block_delta/message_delta  全走  View， thinking/redacted_thinking 命中后才物化   Json 调   appendThinkingBlock 。
    •  message_delta.usage ：显式判   contains("output_tokens") 后再覆盖，避免无usage块误清零。
  •  agent/lib/include/agentxx/agent/io/wire_protocol.h  +   agent/lib/src/agent/wire_protocol.cpp ：
    •  新增   msgTypeView(const JsonView&)  零拷贝取   type 。
    •  deserialize  先   JsonView::parse 路由+探   getDeserializerMap ，未知type直接丢弃不物化；命中后再   to_json() 物化一次走既有   fromJson 。

P1：wire/conversation/config复查 ✅

  •  dump() 保序： util::Json 为   vector<pair<string,Json>> 保序， ChainHash 经   msg.toJson().dump() 指纹确定。
  •  ChainHash 一致： Session::appendViewMessage 对不含 id 内容哈希， restore 先 clear id 再重建，与旧实现一致。
  •  ViewMessage::toJson/fromJson ：角色专属按role输出/解析， Tool/Tip/Interrupt 对应role保证非空， Think 仅有   think 对象时解析， attachments/MediaAttachment
往返一致。
  •  ModelConfig::extraConfig/PluginConfig::args 已为   util::Json ， yamlToJson/yamlToJsonResolveEnv 直出   util::Json ，FFI   mj["extraConfig"] 直接赋值，无  
neograph::json 残留。
P1：体积/nm/性能（ json.md §9.3 ）✅

  •  nm -D exec/plugins/*/lib*.so | grep -ci neograph|yyjson  16插件全   0 ，无泄漏。
  •  体积： linux-debug/exec  debug+ASan未strip故大（ math 9.5M/filesystem 300M/string 373M/codegraph 77M ）， agent/build/output 小一个量级（
math 414K/filesystem 15M/string 14M ），属预期，非回归。
  •  Bench（ Json::parse+DOM  vs   JsonView::parse+路由 ，2万次SSE  payload， -O2 -fsanitize=address ）：
    •  Json 1648.8ms / View 585.4ms，speedup ~2.8x ， acc 一致。

P1：文档 ✅

  •  docs/zh-cn/design.md/plugins.md §5  +   docs/en/design.md/plugins.md §5 ：
    •  agentxx_util 含   json.cpp/json_view.cpp 自主   Json/JsonView ，依赖   PUBLIC 中   neograph/yyjson 改为   simdjson 并注“自JSON自主化起彻底移除”，示例加  
json.h/json_view.h  +  “高频先View路由命中后物化”。
  •  AGENTS.md ：
    •  util列表加   json/json_view, simdjson驱动 ，依赖   PUBLIC 同步去   neograph/yyjson ，加   json.h/json_view.h 说明。
    •  顺手修陈旧链接： docs/zh-cn/plugins.md → docs/zh-cn/design.md/plugins.md ， docs/zh-cn/design.md → index.md ， 4.2/9.3.1/4.5/4.6 → §4/§14/§5 。
  •  TODOS.md ： 使用 simdjson 代替 neograph::json 标   [done] 并注落地范围。

P2：验证 ✅

  •  agentxx_lib_repo-build: agentxx_shared/agentxx_static  通过（仅曾报   safeOutputIndex unused ，已   maybe_unused 消除）。
  •  agentxx_client_repo-build: agentxx_cli  通过。
  •  agentxx_test_repo-build: agentxx_test  零error，已同步   exec/agentxx_test/libagentxxd.so/libagentxx_util.a 。
  •  exec/agentxx_test openai_provider anthropic_provider :   457/0 + 242/0 ； toolcall_args/config_loader/event_bridge 等关键模块通过；全量   18039/0 。

2. 待完成/注意（转交）

  •  P2收尾只剩提交前自查： git diff 重点看   openai_provider.cpp/anthropic_provider.h/wire_protocol.h/cpp/AGENTS.md/TODOS.md/plugins.md ；历史多次重写文件（
neograph_json_bridge.h/ui_control.cpp/codegraph.cpp/tool.h/agent_runner.cpp/wrap_handle.h/middleware.h ）勿再批量   s/neograph::json/util::Json/ 。
    •  逐文件原则仍有效： ChatMessage/ChatTool/GraphState/RunConfig/NodeContext/GraphRegistry extra/channel/input/state 一律回   neograph::json ；业务  
llmMessages/args/body/sent 用   util::Json ；边界显式   toNeographJson/fromNeographJson/parseNeographJson 。
  •  linux-debug 无   plugins_repo 目标， exec/plugins/*.so 易陈旧； websearch/string 曾因独立分支缺   html2md 致   undefined symbol ，已在两处   CMakeLists 独立分支加  
find_package(html2md)+link ，若再现先查该处。
  •  agent_runner.cpp resume_async 必须传   neograph::json{} （与   HEAD/9ec9e95 一致），勿传   toNeographJson(resumeValues) ，否则引擎把非空   resume_value
以user消息写入messages致   message_supplement size=6≠5 。工具中断交付走   interruptResult graphData 。
  •  message_supplement 取证代码已移除，保持移除态。
3. 给下会话的关键提示

  •  快速验证（顶层   /home/coolight/program/agentxx ）：

 bash 
 
  cmake --build agent/build/linux-debug/agentxx_lib_repo-prefix/src/agentxx_lib_repo-build --target agentxx_util -j4
cmake --build agent/build/linux-debug/agentxx_lib_repo-prefix/src/agentxx_lib_repo-build --target agentxx_shared agentxx_static -j4
cmake --build agent/build/linux-debug/agentxx_client_repo-prefix/src/agentxx_client_repo-build -j4
cmake --build agent/build/linux-debug/agentxx_test_repo-prefix/src/agentxx_test_repo-build -j4
cp -v agent/build/linux-debug/agentxx_test_repo-prefix/src/agentxx_test_repo-build/agentxx_test agent/build/linux-debug/exec/agentxx_test
cp -v agent/build/linux-debug/agentxx_lib_repo-prefix/src/agentxx_lib_repo-build/libagentxxd.so agent/build/linux-debug/exec/libagentxxd.so
cp -v agent/build/linux-debug/agentxx_lib_repo-prefix/src/agentxx_lib_repo-build/libagentxx_util.a agent/build/linux-debug/exec/libagentxx_util.a
 
 

  •  测试必须在   agent/build/linux-debug/exec 下跑（ plugins/ 相对路径），否则插件加载失败误报：

 bash 
 
  agent/build/linux-debug/exec/agentxx_test openai_provider anthropic_provider
agent/build/linux-debug/exec/agentxx_test
 
 

  •  libsimdjson.a 带ASan，独立冒烟需   -fsanitize=address ，另需显式链   libsimdjson.a  +   libfmtd.a 。
  •  ItemsIterable first 是   string_view ，存   map<string> 需包   std::string{} 。
  •  neograph::json 与   util::Json 无隐式转换； value(key,def) 有   const char* 重载， get<T> 需特化齐全； JsonView::operator[] 缺失返回无效视图不抛， get_string_view

---

## 本会话增补 (2026-09-08 晚, 接上文继续执行)

### 语义对齐修复 (openai_provider.cpp, 未暂存)
- `parseUsageView` 细节分支改回 `contains(key)` 守卫: 非对象 details(如字符串)视为未命中换下一分支,与原 `parseUsage` 一致。
- `viewOutputIndex` 去掉 double 分支: 与 `safeOutputIndex`(仅 `is_number_integer`)严格一致,浮点视为缺失→0。
- Chat `tool_calls[index]` 去掉 double 分支: 仅整数+字符串数字有效,与原 `is_number_integer` 一致。
- Responses 错误分支: `error` 为字符串时改回整包 `dump`(与原 `else j.dump()` 一致);注释已写明三分支语义。

### 交接卫生
- `TODOS.md` 已恢复为 HEAD(29行): 工作区长篇总结移至本文档,不再污染 TODOS。
- 交接基线更新: HEAD 仍 `f03d1885`, 工作区约 `197 files (190 staged + 7 unstaged)`,
  unstaged 为 `AGENTS.md/wire_protocol.h/anthropic_provider.h/wire_protocol.cpp/openai_provider.cpp/中英plugins.md`。

### 验证 (本会话实测)
- `agentxx_shared/agentxx_static/agentxx_cli/agentxx_test` 构建零 error;二进制已同步 `exec/`(md5一致)。
- `exec/agentxx_test openai_provider anthropic_provider` → `699/0`;
  `toolcall_args config_loader event_bridge` → `395/0`。
- 全量 `exec/agentxx_test` 本会话重跑未通过:崩溃在 `regex` 模块 hyperscan `hs_compile` 的 ASan `unknown-crash`(栈见 `rose_build_lookaround.cpp`),与 JSON 改动无关(本会话仅改 openai SSE View);`regex` 为 hyperscan 第三方已知问题,待另行处理。全量命令见 json.md §9.2。
- `nm -D exec/plugins/*/*.so | grep -ci neograph|yyjson` → `0`(grep无匹配,exit=1属预期)。
- 独立冒烟(`-O2 -fsanitize=address`,2万次SSE payload): `JsonDOM 1739ms / View 413ms,speedup~4.2x,acc一致`;
  小 payload 复用 parser(`JsonView::parse(sv,parser)`,5万次): `DOM 1002ms / REUSE 300ms,acc一致`。
- `§9.1 test_json/test_json_view/test_json_reflection` 仍缺失:测试靠 GLOB 自动收录,
  补单测只需新增 `agent/test/core/test_json*.cpp/.h` + `test.cpp` 注册,无需改 CMake。

### 待办
1. 全量测试:先跳过 `regex` 回归其余模块(本会话 `openai/anthropic/toolcall_args/config_loader/event_bridge/string_util/diff_util/events` 组合 `1094/0`、`579/0`);`regex` 崩溃另建任务跟进。
2. 提交前自查 `git diff` 7个 unstaged 文件(重点看本会话4处语义修复)。
3. 后续再补 `§9.1` 单测与 `§5` 反射(当前 `AGENTXX_HAS_CPP26_REFLECTION=0` 为降级路径)。
