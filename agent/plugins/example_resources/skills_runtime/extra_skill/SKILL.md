---
name: extra-skill
description: Demo skill registered at runtime by the example_resources plugin entry via the vtable register_skill_dir API.
---

# Extra Skill

A demo skill contributed by the `example_resources` plugin at runtime
(vtable `register_skill_dir`, not the declarative manifest section).

Workflow:

1. Confirm you can see this skill.
2. Explain that it was registered by plugin code during load, not by plugin.yaml.
