# TODO
- 向 fullHistory 插入中断、掉线等提示消息
- tui 特化各种 toolcall 渲染

- 用户输入队列；
    - deepagent正在执行会话时，如果用户输入内容并发送，则添加到输入队列中，一旦 deepagent 执行会话完成，自动取一个输入发送给deepagent 继续去执行
    - 如果输入队列非空，在 TUI 中的输入框上方显示 `待发送消息: {队列数量}`, 点击可弹窗查看待发送消息队列，弹窗顶部为两端对齐 `待发送消息队列   {清空按钮，点击清空待发送消息队列，并关闭弹窗}`，每个消息默认折叠为一行显示，每个消息右边有一个删除按钮，点击可以从消息队列中移除这条消息
- 修改当屏幕宽度足够大时，默认显示`信息`侧边栏，内容布局:
    - 如果有planning toolcall正在规划执行，特化渲染planning
    - 底部显示:
        - 当前程序工作目录
        - Agentxx ${版本号} ${显示运行模式: 内置Agentxx/远程Agentxx的 http[s]://Ip:端口}